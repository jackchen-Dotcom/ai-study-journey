# 错题本

> 记录规则（2026-09-04 修订）：**所有做的题都记一条**——磕绊的题详记（错因+坑），一次通过的题记「思考复盘」；每条支持多次复盘（⬜ 逐次打勾推进）。
> 错因只分三类：「思路没有」（完全没方向）、「思路有但不对」（方向错了）、「实现失误」（思路对但写错了）。

---

## 2026-09-11 | 92. 反转链表 II | 标签: 链表（区间反转 · 硬核题单首题 · 4 版跨两天）
- 我的原代码: 见 records/2026-09-11_92_反转链表II.py（v1 三伤存档 → v4 三定位+两刀接缝通过）
- 错因分类: 实现失误为主（v1 框架就有：定位+段内反转+接缝的直觉）+ 一处语义混淆（== vs !=）
- 坑在哪:
  1. **⭐⭐ `==` 语义写反 → 循环零次执行**：`while cur == p2.next` 首次 3==5 即 False，"没有反转"的直接死因；且比较对象也错（应 `!= p2`）。症状=纹丝不动 → 先查循环体有没有跑
  2. **前驱角色缺失（连坑两次）**：v2 用 `dummy.next = p2` 接新头——dummy 只在 left=1 时才是前驱，left>1 时它不在链上，整段挂空；v3 声明 `prev = dummy` 后**忘了让它走**——变量声明 ≠ 到位
  3. **起点漏 left 位**：`cur = p1.next` 从 left+1 开始，left 位自己没被反转
  4. **"1 没了"诊断法**：症状是节点整段挂空 → 查它该被谁接 → 查该站位的指针走没走到位（print(prev.val) 验位）
- 正确思路: **三定位**（prev 走 left-1 步停前驱 / p1=prev.next / p2 走 right 步）→ **段内四行铁律**（while cur != p2，故意漏 p2）→ **两刀接缝**（p2.next=pre：right 位归队，此时 pre 已演变 5→2→3；prev.next=p2：前驱接新头）→ return dummy.next
- 我的思考信号: "区间反转" = #206 引擎 + 接缝工程；单向链表铁律"想改谁的 next 就站谁的位"——接缝必须提前占位；循环故意留最后一个节点、退出后补刀；return 永远是新入口（dummy.next），head 变量≠链表头
- 直系同族: #25 K 个一组翻转（本技总考）、#206（段内引擎）、#61（下刀法对照：不反转只搬移）、#234（找中点+反转后半）
- 复盘状态: ✅ 2026-09-11 通过（4 版，预期 2~4 版符合）| ⬜ 二刷（三定位+两刀骨架默写，重点默 prev 走步）| ⬜ 三刷（头插法对照：摘 nex 插 prev.next，固定 right-left 轮，无补刀）

## 2026-09-10 | 206. 反转链表 | 标签: 链表（三指针接力 · 面试 No.1 高频）
- 我的原代码: 见 records/2026-09-10_206_反转链表.py（第1版断链存档 → 第2版标准解一次修正通过）
- 错因分类: 实现失误（思路对：三指针反转；但前进顺序错 + 返回值选错）
- 坑在哪:
  1. **⭐⭐ 两个名字一个节点**：cur 和 nex 都初始化为 head，循环末又 cur=nex——全程同步，三指针退化成两指针；第一步 cur.next=pre 掐断 next 后，nex=nex.next 取到的是刚改的指针 → [1,2,3] 循环只跑 1 轮，2、3 没被摸过
  2. **nex 的本职**：在 next 被改掉**之前**先把后继存起来（每轮第一个动作），防止断链——它不是第二个同步指针
  3. **返回值要选"活着的"**：退出循环时 cur=None，新头在 pre——return 谁要看退出时谁攥着答案
- 正确思路: 四行铁律顺序——①存（nex=cur.next）②改（cur.next=pre）③平移（pre=cur）④前进（cur=nex）；退出 return pre。pre 初始 None 的深意：反转后原头（新尾）.next=None，防成环
- 我的思考信号: 见"反转链表" → pre/cur/nex 三指针接力 + "断链前先存" + 画 3 节点图手推第一轮；返回值在循环外挑"最后还指着节点的那个"
- 背景注: 用户学 C 时做过此题——指针概念迁移是思路快的来源；但第 1 版仍踩"同步指针断链 + 返回值"两坑 → **旧经验给骨架，坑还得自己踩**（Python 引用语义 = C 指针的自动挡）
- 直系同族: #92 反转链表 II（区间反转，硬核题单）、#234 回文链表（找中点+反转后半，接 #876）、#25 K 个一组翻转（链表总考）、#61 旋转链表（下刀法对照）
- 复盘状态: ✅ 2026-09-10 通过（2 版）| ⬜ 二刷（四行骨架遮住默写，30 秒内）| ⬜ 三刷（#234 检验：反转后半段拼回文判断）| ⬜ 递归版（用户自评当前学不会，正常搁置——先在阶乘/#70 递推式建立"信仰之跃"体感，再回打链表递归；要点已讲：new_head=子链新头、head.next.next=head 尾巴回头拉、head.next=None 防环）

## 2026-09-09 | 70. 爬楼梯 | 标签: 动态规划（DP 第一课 · 双解）
- 我的原代码: 见 records/2026-09-09_70_爬楼梯.py（枚举数学版（自己的逻辑修正）→ DP 版，全通）
- 错因分类: 思路有但不对（计数公式 x*(2y) 非排列数）+ 实现失误（漏 y=0、奇偶补丁）
- 坑在哪:
  1. 枚举"1步 x 个 + 2步 y 个"的排列数 = **组合数 C(x+y,y)**（x+y 个位置选 y 个放"2"），不是 x*(2y)——n=4,y=1 手验：112/121/211 共 3 种，公式给 4
  2. range(1,n//2) 漏 y=0（全 1 步方案）且不含上界 y=n//2
  3. 奇偶分支+tmp<=3 特判全是给错误公式打的补丁——**修根因别叠补丁**（#19 教训复用）
- 正确思路: DP 五步法（patterns「动态规划入门」）——①dp[i]=到第 i 阶方法数 ②dp[i]=dp[i-1]+dp[i-2]（最后一步迈 1 或 2，无第三来源，即那句英文翻译）③dp[1]=1,dp[2]=2 ④从小到大 ⑤dp[n]；空间优化 prev/curr 只滚前两阶
- 我的思考信号: 问"多少种方法"→ 计数型 DP；"最少/最多"→ 最值型；"能否"→ 可行型。**问"数"不问"路"**
- 直系同族: 62.不同路径、53.最大子数组和、322.零钱兑换、198.打家劫舍、139.单词拆分
- 复盘状态: ✅ 2026-09-09 双解通过 | ⬜ 二刷（五步法默写 + 遮住写滚动版）| ⬜ 三刷（变式：允许一次爬 1/2/3 阶，转移方程怎么改）

## 2026-09-08 | 67. 二进制求和 | 标签: 数组/字符串（进位模拟 · 磨 6 轮的硬仗）
- 我的原代码: 见 records/2026-09-08_67_二进制求和.py（修复版置顶 + 官方题解对照 + 坑史全集）
- 错因分类: 实现失误连环（对齐公式反转 / join 误用 / 方向 / carry 位置 / 前导零）——核心 carry 机制（%2//2）首版即对
- 坑在哪:
  1. **⭐⭐ 对齐公式镜像反转（事故核心）**：aarr[n-1-i]=int(x) 把整串倒放（enumerate 从最高位字符起，n-1-i 送最低位）；正确公式 aarr[n-len(a)+i]。**AI 推演也翻车**："看起来对"+脑内走查均判对，实测 10010 才现形——推演一百遍不如跑一遍，测试用例是唯一真相
  2. **join 语义**：''.join(序列)，胶水空串 + 元素全 str；''.join(str(x)) 单字符=没拼；reversed 返回迭代器不是 str，力扣直接拒收
  3. **方向两连错**：range(len) 从高位开始加——进位向高处传，必须 range(n-1, -1, -1)
  4. carry 检查放循环外；dig 多开格仅进位时输出（否则前导零："11"+"1" 出 "110"）
- 正确思路: 官方对照双流派——反转世界（a[::-1] 后下标=从低位数第 i 位）vs 坐标换算世界（n-len+i 右对齐）
- 我的思考信号: 两个数字串/数组逐位加 → 进位模拟三件套：对齐（反转或偏移）+ 逐位 sum%2//2 + 尾进位收尾
- 直系同族: #66 加一（十进制版骨架）、#415 字符串相加、#43 字符串相乘
- 复盘状态: ✅ 2026-09-08 通过 | ⬜ 二刷（进位骨架遮住写，5 分钟内完成）| ⬜ 三刷（#415 检验迁移）

## 2026-09-08 | 61. 旋转链表 | 标签: 链表（下刀法·测验 T3 重做）
- 我的原代码: 见 records/2026-09-06_61_旋转链表.py（测验首版四伤 → 9/8 合上重写一次通过）
- 错因分类: 思路有但不对（定位在正数端，右旋新头在倒数端）+ 实现失误（类无括号、踩尾=None、无 k%len 短路）
- 坑在哪:
  1. **坐标系没换算（核心）**：k 数的是新链坐标（前面搬来 k 个），下刀用原链坐标——倒数第 m = 正数下标 length−m，新尾走 length−k−1 步。旋转题的坑全在坐标系统一
  2. **踩尾退出**：while p1: p1=p1.next 退出 p1=None；用 while p1.next 停尾
  3. **类无括号**（第 2 次踩，速查卡已立）；无 k%len==0 短路；接线对象错（原尾应接 head 闭环，不是接 p2）
  4. **⭐ 卫语句 guard clause**：not head or k==0 or not head.next——拦"不需要干活"的情况，主逻辑放心假设健康输入；or 短路求值 → 条件顺序=依赖顺序
- 正确思路: 下刀法三步——①闭环（尾接 head）②走 length−k−1 步找新尾 ③新头=新尾.next，断开。k%=length 吞三分支
- 我的思考信号: "旋转/搬移 + 可能 k>长度" → 取模 + 找倒数定位 + 闭环下刀；动笔前画 5 结点方框图标新旧头尾
- 复盘状态: ✅ 2026-09-08 重写通过（测验闭环）| ⬜ 二刷（默写三步骨架 + 口述坐标系换算）| ⬜ 三刷（左旋版本变式）

## 2026-09-07 | 66. 加一 | 标签: 数组（进位模拟 · 双解对照）
- 我的原代码: 见 records/2026-09-07_66_加一.py（数学取巧版一次通过 → 思考题进位版自写通过）
- 我的思考（复盘核心）:
  1. **数学视角取巧**：数组=大整数的十进制表示，+1 后拆回——[9,9,9] 的连环进位被大整数天然消化，一行进位逻辑不用写
  2. **取巧的前提认知**：Python int 无位数上限；Java/C++ long long ≈19 位上限，100 位数字中途溢出——**面试两把刷子：会取巧 + 知道标准解**
  3. **进位版骨架**：末位是 9 → 归 0 进位往前；非 9 → +1 收工；一路 9 → 新数组 [1,0,...,0]。这副骨架就是 #67 二进制求和的前置
  4. **拦截模式迁移**：`len1==0 and digits[0]==9` 在负索引前设卡（Python 负索引不报错才更危险）——#35 "先判界再访问"跨题复用
- 小修: 末尾 return 死代码可删；and 右半冗余
- 我的思考信号: "数字逐位存数组 + 进位/加法" → 进位模拟（末位起，9 归 0 非 9 收）；Python 大整数可取巧但要知道边界
- 直系同族: #67 二进制求和（同一骨架，二进制版）、#43 字符串相乘、#415 字符串相加
- 复盘状态: ✅ 2026-09-07 双版通过 | ⬜ 二刷（进位版默写）| ⬜ 三刷（#67 检验骨架迁移）

## 2026-09-07 | 58. 最后一个单词的长度 | 标签: 字符串（状态机 + 反向遍历）
- 我的原代码: 见 records/2026-09-07_58_最后一个单词的长度.py（正向版一次通过 → 思考题反向版自写通过）
- 我的思考（复盘核心）:
  1. **双状态变量状态机**：t=当前单词长度（易失，空格清零）/ tmp=最近完整单词长度（持久）——tmp 的"不清零"就是正确性来源；正向扫全串 O(n)
  2. **反向版自己迁移 patterns 第一条**（反向遍历+状态变量）：`tmp != 0` 之门化解尾部空格陷阱 + 提前 return——1GB 字符串只扫最后几个字符
  3. **三权分立的另一面**：反向版中 t 与 tmp 永远相等（职责重合）→ 单变量即可。职责独立才拆，重合即合
- 小修: 互斥 if/if 可改 elif；return 前的 `t = 0` 无意义可删
- 我的思考信号: "找最后一段/最后一个满足 X 的段" → 反向遍历 + 提前返回；分隔符切换状态 → 双状态变量状态机
- 直系同族: #434 字符串中的单词数（状态机原样复用）、#125 验证回文串（双指针扫字符串）
- 复盘状态: ✅ 2026-09-07 通过（正向+反向两版）| ⬜ 二刷（单变量版重写）| ⬜ 三刷（#434 检验状态机迁移）

## 2026-09-06 | 977. 有序数组的平方 | 标签: 双指针（相向流派·测验 T1 重做）
- 我的原代码: 见 records/2026-09-06_977_有序数组的平方.py（测验首版 IndexError → 重做两轮通过）
- 错因分类: 思路有但不对（二分套 U 形非单调数组）+ 实现失误（引用污染、变量超载、!= 当门）
- 坑在哪:
  1. **套模板前不验前提**：平方后数组是 U 形（先降后升）非单调——二分前提崩，必须换武器
  2. **`result = nums` 是贴标签不是拷贝**：写 result 就是写 nums，原数组被污染 → `[0]*len(nums)` 新建
  3. **⭐ 变量三权分立（我的洞察，已进 patterns）**：i 一人分饰三角（左指针/右端偏移/填充计数）→ 拆成 left/right/fill；乱不乱不看变量个数，看职责有没有归属
  4. **门写反 `while left >= right`**：循环一次不进直接返回原数组；== 时还剩中间元素，必须 <=
- 正确思路: 相向双指针——最大平方只能在两端，谁大谁填 result 末尾（从后往前），指针内移；填位每轮 -1
- 我的思考信号: "有序 + 平方/绝对值 + 结果也要求有序" → 两端向中心；对比快慢同向（#26/#27/#283）
- 复盘状态: ✅ 2026-09-06 重做通过 | ⬜ 二刷（遮住写 + 口述"为什么填末尾不是开头"）

## 2026-09-06 | 69. x 的平方根 | 标签: 二分（测验 T2 · 断层线第三形态）
- 我的原代码: 见 records/2026-09-06_69_x的平方根.py（测验线性版通过 → 二分重做通过）
- 错因分类: 实现失误（^ 误作幂、值条件死循环、mid//2 收缩错）+ 思路有但不对（return left 断层线反向）
- 坑在哪:
  1. **`2^16` 是异或（=18）不是幂**——Python 幂是 `**`；其他语言肌肉记忆坑
  2. **值条件死循环复发**：`while mid*mid < x`——与 #35 首版 `while target != nums[mid]` 同病根；门上只挂 `left <= right`，值写在循环体三分支
  3. **收缩写成 `mid//2 ± 1`**：mid 已是中点，收缩 = 边界 ±1，雷打不动
  4. **断层线方向**：#35/#278 找"第一个满足"→ `return left`；#69 找"最后一个 i*i ≤ x"→ `return right`（= left−1）
- 正确思路: 三分支比较 mid² vs x + 范围条件 + return right；上界 65535 ≥ √(2³¹−1)≈46341
- 我的思考信号: "找最后一个满足某性质" → 答案 = right；"找第一个满足" → 答案 = left——先问方向再写 return
- 复盘状态: ✅ 2026-09-06 测验（线性通过，O(√x)）| ✅ 二分重做通过 | ⬜ 三形态默写（#35/#278/#69 一表背出断层线方向）

## 2026-09-05 | 278. 第一个错误的版本 | 标签: 二分（同族加练·模板迁移检验）
- 我的原代码: 见 records/2026-09-05_278_第一个错误的版本.py（一次通过）
- 我的思考（复盘核心）:
  1. **二分的灵魂 = 区域性质不变量**：left 左边永远全好、right 右边永远全坏（每轮收缩都在给扔出去的区域"盖章"）；退出 left=right+1 时混合区消失，断层线 = 最后的好与最先的坏的分界 = 第一个坏版本
  2. **与 #35 对照**：同一模板，断层线含义随"扔出去的条件"变化——#35 是插入位置，#278 是第一个坏版本。模板通用性的本质：只定义"往哪边扔"，答案自动浮现在断层线上
  3. 疑问"mid 是第一个坏版本时被扔出去会不会丢"：不会——它进 right 右侧坏区，断层线最终精确回到它（n=5, bad=3 手走验证）
- Pythonic 修正: `== False / == True` 是废话比较 → `if not ...` / `else`（布尔自带条件语义）
- 我的思考信号: "找第一个满足某性质的" + 单调分界（好|坏、≤|>、能|不能）→ 二分，答案 = 断层线
- 复盘状态: ✅ 2026-09-05 通过 | ⬜ 二刷（遮住写 + 口述两条区域性质）| ⬜ 三刷（预测 #69 的断层线含义再验证）

## 2026-09-05 | 35. 搜索插入位置 | 标签: 二分（入门题·朴素→优化两版对照）
- 我的原代码: 见 records/2026-09-05_35_搜索插入位置.py（第1版线性扫描通过 → 最终版二分通过 66/66，0ms 击败 100%）
- 错因分类: 实现失误（右边界溢出、左边界漏判）+ 思路有但不对（终止条件靠"特判残局"而非"范围含义"）
- 坑在哪:
  1. **右边界溢出**：访问 `nums[i+1]` 前没判 `i+1 == len`——教训：**先判界，再访问**，顺序即生命
  2. **左边界漏判**："插在头部"循环覆盖不了 → 前置哨兵特判（与哑结点同族思想：特殊情况挪到循环外消灭）
  3. **终止条件补丁循环**：从"差距1返回右"到"相等返回右+1"连换两个特判——根因是没定义**范围的含义**；统一解 = `while left <= right`（== 时还剩 1 个没查）+ 退出 `return left`，零特判
  4. **⭐ / 与 // 的语言版本坑（今日最大收获）**：力扣语言选 "Python"(=2.7)，`/` 对两整数是整除 → 通过；Py3 的 `/` 返回 float，`nums[1.5]` 当场 TypeError。Py2 已死(2020)，我以后一切代码按 Py3 语义：**mid 必须 //**
- 正确思路: 有序数组找插入点 = 找第一个 ≥ target 的下标。left/right 维护候选范围 → 比较中点 → 淘汰一半 → 范围反转瞬间 left 即答案
- 我的思考信号: 有序 + 找位置/边界 + 数据量大 → 二分。O(n) 每轮淘汰 1 个 vs O(log n) 每轮淘汰一半（10^18 数据 60 轮搞定）
- 复盘状态: ✅ 2026-09-05 通过（线性→二分两版进化）| ⬜ 二刷（遮住写二分 + 口述"为什么 return left 与 right+1 等价"）| ⬜ 三刷（把 / 版切到 Python3 提交，亲眼看一次红）

## 2026-09-04 | 876. 链表的中间结点 | 标签: 链表（快慢指针·差速流派）
- 我的原代码: 自创防御式写法（dummy + 循环内双重检查 + 提前 return），力扣全用例通过
- 我的思考（复盘核心）:
  1. 我的不变量：循环末尾双重检查 → 保证每轮开始时 fast 后面至少还有 2 个结点 → `fast.next.next` 永不踩空
  2. 两种退出自动对应奇偶：偶数链 fast **跨出链表**（=None）退出 → slow 已站上第二个中间；奇数链 fast **踩尾**（fast.next=None）退出 → slow 在正中间。代码不判断奇偶，奇偶自己选出口
  3. 与经典三行版对照：`while fast and fast.next` 把防御搬进循环条件（先检查后迈步）；我的版检查放循环内（先迈步末尾验伤）——防御等价，经典版更短
  4. 「补一步」的位置：我的 slow 从 dummy 出发 → 返回 slow.next 补一步；经典版从 head 出发 → 起跑线前移一格，直接 return slow。**位移相同，补的时机不同**
  5. while 执行模型：检查只在每轮开头，条件为假挡住的是**下一轮**，正在跑的这轮一定合法
- 我的思考信号: 找中点/找相遇 → 差速（快2慢1）；跳两步之前确认脚下两格都在
- 复盘状态: ✅ 2026-09-04 一次通过 | ✅ 二刷 2026-09-05（经典三行版重写完成，已存档为最终版）| ⬜ 三刷（口述不变量证明）

## 2026-09-04 | 203. 移除链表元素 | 标签: 链表（哑结点 + 接线删除）
- 我的原代码: dummy + p1(前驱)/p2(当前)，一次通过（含四个刁钻边界静态验证：连删到空/删头/空表/隔位删）
- 我的思考（复盘核心）:
  1. 删除发生的那轮 p1/p2 都不动，下一轮 `p2 = p2.next` 却恰好滑到正确位置——因为**「删除」没有消灭结点**：主链绕过它，它自己的 .next 还指着原来的下一个（幽灵结点借道）
  2. 我写的时候是"恰好写对"，复盘追问"为什么对"之后变成有意为之——**做对之后还要能解释对**，才算真的会
- 与 #19 的关系: #27 的链表版，哑结点 + 接线删除直接复用
- 我的思考信号: 链表删"所有满足条件的结点" → 哑结点 + prev/cur，prev 只在"不删"时前进
- 复盘状态: ✅ 2026-09-04 一次通过 | ⬜ 二刷（试 while cur 经典写法对比）

## 2026-09-04 | 19. 删除链表的倒数第N个结点 | 标签: 链表（哑结点 + 快慢指针·同速错位）
- 我的原代码: 见 records/2026-09-04_19_删除链表的倒数第N个结点.py（5版通过 5/5）
- 错因分类: 思路有但不对（把快慢指针当差速用）+ 实现失误（移动局部变量当成删除）
- 坑在哪:
  1. **移动局部变量 ≠ 修改链表**：`p1 = tmp.next` 只改局部指针指向，链表零修改（第1、2版 0/5 全报原链表的根源）；真删除 = `p1.next = p1.next.next` 接线赋值
  2. **无哑结点处处差一位**：p1 停在被删结点自己而非前驱；删头要 3~4 行特判——dummy 把边界计算卸载给一个结点
  3. **计数先加后移多算 1**：count 变成 len+1；用 count=-1 起步等效修正（或改先移动后计数）
  4. **特判补丁思维**：if count > n 因题目保证 n ≤ len 而恒真，else 成死代码——修根因，别叠补丁
- 正确思路: dummy 接头 → 双指针从 dummy 同速出发，p2 先走到 None → p1 落后 n 正停前驱 → 接线删除 → return dummy.next
- 我的思考信号: 链表找/删"倒数第 k" + 可能删头 → 哑结点 + 同速错位 n；快慢指针两流派：差速找相遇（#141）vs 同速错位找前驱（本题）
- 复盘状态: ✅ 2026-09-04 通过（5/5）| ✅ 二刷 2026-09-05（优化为真·一趟扫描版：fast 先走 n 步拉开差距 + 同步走 + while p2.next 踩尾退出，零特判零计数，已存档为最终版；与 #876 经典版同构）| ✅ 默写挑战 2026-09-10（count 版骨架全对、5 边界走查全过；结尾三 if 发胖：1 死代码 + 2 可归约——**p2.next=p2.next.next 一行通吃跳接与删尾，None 替你干分类讨论**）| ⬜ 三刷（遮住两版，口述"错位-同步-退出"骨架）

## 2026-09-03 | 283. 移动零 | 标签: 双指针（快慢·收尾变体）
- 我的原代码: 见 records/2026-09-03_283_移动零.py（4版通过）
- 错因分类: 思路有但不对（想一步做完"搬运+清零"两件事）
- 坑在哪:
  1. **slow==fast 时清零 = 毁原件**：`nums[fast]=0` 只有在该槽位已被复制到前面时才合法
  2. 卡住时叠加补丁（加条件、多写一次）而不是回头拆步骤
- 正确思路: **两阶段**——第一阶段= #27 框架搬非零值；第二阶段 slow 之后统一填 0
- 我的思考信号: 原地 + 一遍走不通时 → 拆步骤（搬运/清理分离）
- 复盘状态: ✅ 2026-09-03 通过

## 2026-09-03 | 26. 删除有序数组中的重复项 | 标签: 双指针（快慢）
- 我的原代码: 见 records/2026-09-03_26_删除有序数组中的重复项.py（3版通过 5/5）
- 错因分类: 思路有但不对（v1 无搬运动作）→ 快指针卡死（v2）
- 坑在哪:
  1. **for 循环变量本身就是快指针**：`for _ in range(...)` 里快指针不前进会卡死；`for highs in range(1, len(nums))` 一行解决
  2. **写 0 = 毁数据**：原地去重是"搬运不重复的"，不是"清零重复的"（0 本身是合法数据）
  3. **个数 = 最后不重复下标 + 1**：返回 lows+1 不是 lows
  4. 元素 vs 下标第 3 次栽（v1 的 `for n in nums`）
- 我的思考信号: 原地数组操作 + "保留区/待检查区"两段结构 → 快慢指针；慢=保留区末尾，快=探测
- 复盘状态: ✅ 2026-09-03 通过

## 2026-09-02 | 21. 合并两个有序链表 | 标签: 链表（复盘验证题）
- 我的原代码: 见 records/2026-09-02_21_合并两个有序链表.py（默写2版通过 5/5）
- 错因分类: 实现失误（骨架默写正确，2处细节错）
- 坑在哪:
  1. `ListNode[0]` → `ListNode(0)`：**()是"调用/创建"，[]是"取值/下标"**——与 #13 的 `roma_map(x)` 同族错误，第2次犯
  2. 循环结束后剩余链尾丢失：`while 双方都不空` 停止时必有一方没走完，需 `tail.next = 剩余整队`
- 我的思考信号: 两条有序序列要"穿插合并" → 哑结点 + tail 游标 + 双指针逐个比较；一方打空后整队并入
- 复盘状态: ✅ 2026-09-02 默写通过（骨架完整，质量比 #20 默写好）| ✅ 2026-09-05 三天复盘默写通过（自带空表特判+意图注释）；主动追问两个原理：①`<=` vs `<`（稳定性）②while 条件为何守 `p1 and p2` 而非 `.next` → 已理解"循环条件守循环体内要碰的东西"

## 2026-09-02 | 20. 有效的括号 | 标签: 栈（复盘验证题）
- 我的原代码: 见 records/2026-09-02_20_有效的括号.py（默写3版后通过 8/8）
- 错因分类: 思路有但不对（流程缺失）+ 实现失误
- 默写暴露的记忆盲区:
  1. **漏了入栈动作**: 只记得"查映射表"，忘了左括号要先压栈——算法的骨架步骤必须完整
  2. **`stack is None` ≠ 空列表**: 判空要用 `if not stack:`，`is None` 永远为 False
  3. **两个平行 if**: 入栈后紧接着无条件 pop，把刚压的又弹了——左/右括号两条路必须 if/else 互斥
  4. **空栈 pop → IndexError**: pop 前必须先判栈空（"]"用例）
  5. **结尾判断写反**: 栈空=全部配对=有效，`return not stack` 一句搞定
  6. 语法: 赋值 `=` vs 比较 `==`；`return` 后直接跟表达式
- 我的思考信号: 出现"最近的一个必须先匹配/嵌套结构" → 用栈
- 复盘状态: ✅ 2026-09-02 默写通过 | ⚠️ 2026-09-05 三天复盘：**首次默写未通过**（卡死求援），暴露残留盲区：①字典取值语法第 3 次踩坑（`()`=调用 vs `[]`=取值，#13/#21 同族）②骨架仍需提示才能拼全；重学后修正版与已通过代码一致 → 通过 | ⬜ 2026-09-06 测验卷变式再验 | ⬜ 新增速查卡：`()=调用，[]=取值`

## 2026-09-02 | 14. 最长公共前缀 | 标签: 字符串
- 我的原代码: 见 records/2026-09-02_14_最长公共前缀.py（迭代 3 版后通过）
- 错因分类: 思路有但不对（第1版）→ 实现失误 + 边界缺失（第2版）
- 坑在哪:
  1. **元素 vs 下标混用**: `for i in strs` 拿到的是字符串却当整数下标用 → TypeError
  2. **`in` ≠ 前缀匹配**: `in` 是"任意位置包含"，前缀要用 `startswith()`
  3. **返回了失败者**: 验证失败时返回的是当前(失败的)候选，应返回"最后一次成功"的 → 需要一个 `ans` 变量记录
  4. **切片端点 off-by-one**: `minstr[:i]` 永远验证不到完整 minstr → `[:i+1]`
  5. **变量未初始化**: minstr 为空串时循环不执行，`return ans` 报 UnboundLocalError → 初始化必须提到循环外
- 正确思路(我的): 横向收缩——找最短字符串作候选，逐个验证 startswith，不通过就收缩
- 我的思考信号: 上一位与下一位有强烈关联 / 候选答案需要逐步验证收缩
- 复盘状态: ✅ 2026-09-02 通过（力扣全用例）| ⚠️→✅ 2026-09-05 三天复盘：首默挂（语法2+逻辑3：解包未用enumerate、缺冒号、不匹配不停、负索引、全匹配漏结局）→ 按修改思路重写通过；**亮点：纵向扫描思路是自己长出来的**（逐列比较，比当时的横向收缩更优）| ⬜ 二刷：①去掉 ans 中转，直接 return 切片 ②min() 预限列数消掉边界检查 | ⬜ 三刷：不看任何版本，从"逐列比较"四字重写

## 2026-09-02 | 13. 罗马数字转整数 | 标签: 哈希表/字符串
- 我的原代码: 见 records/2026-09-02_13_罗马数字转整数.py（第2次提交已通过 6/6）
- 错因分类: 实现失误（思路本身正确）
- 坑在哪:
  1. 循环体缩进跑出了方法体 → SyntaxError
  2. 字典取值写成 `roma_map(x)`，应为 `roma_map[x]`
  3. `cur = pre_val` 赋值方向写反，应为 `pre_val = cur`
- 正确思路: 从右往左遍历，当前值 < 前一个值则做减法，否则做加法；用 pre_val 记录上一个值
- 复盘状态: ✅ 2026-09-02 当场修复通过 | ⚠️ 2026-09-05 三天复盘：默写完成但后半段（逻辑层）自我判定"像死记硬背"——是背诵式复现，不是理解式重建 | ⬜ 二刷改玩法：不看旧代码，只给罗马数字规则原文，从规则**重新推导**算法；推不出来 = 回炉"为什么从右往左"
<!-- 示例格式（做完第一道错题后仿照填写，可删除此注释）
## 2026-09-01 | 1. 两数之和 | 标签: 哈希表
- 我的原代码: 见 records/2026-09-01_两数之和.py
- 错因分类: 实现失误
- 坑在哪: 双层循环时内层 j 从 0 开始，导致用了同一个元素两次
- 正确思路: 遍历时把"target - 当前值"存入字典，边查边存
- 复盘状态: ⬜ 3天后重做
-->
