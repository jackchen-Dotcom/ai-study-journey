# 283. 移动零

- 题号: 283
- 难度: 简单
- 标签: 数组, 双指针

## 题面

给定一个数组 `nums`，编写一个函数将所有 `0` 移动到数组的末尾，同时**保持非零元素的相对顺序**。

要求**原地**修改（不能复制数组），无返回值。

示例：`[0,1,0,3,12]` → `[1,3,12,0,0]`。

判题说明：用例带 `"convert": "array_inplace"`，只验证数组最终状态。

## 测试用例

```json
[
  {"input": [[0, 1, 0, 3, 12]], "expected": [1, 3, 12, 0, 0], "convert": "array_inplace"},
  {"input": [[1, 0, 2]], "expected": [1, 2, 0], "convert": "array_inplace"},
  {"input": [[0, 0]], "expected": [0, 0], "convert": "array_inplace"},
  {"input": [[1]], "expected": [1], "convert": "array_inplace"},
  {"input": [[2, 1]], "expected": [2, 1], "convert": "array_inplace"},
  {"input": [[0, 0, 0, 1]], "expected": [1, 0, 0, 0], "convert": "array_inplace"}
]
```

## 笔记（判题后补充）

- （做这道题时踩的坑、学到的思路，做完后写在这里）
