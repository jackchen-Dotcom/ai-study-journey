# AGENTS.md — 本项目 AI 助手协作规则

> 本文件对 AI 助手强制生效。用户指定的规则，违反即为错误。

## 规则 1：规划必须建立在最新知识上（强制 · 用户 2026-09-03 指定）

**触发条件**：任何涉及以下内容的任务——
- 学习路线 / 月度计划 / 每周清单的制定或调整（roadmap.md、daily.md、plan.md）
- 技术选型（框架、库、工具、项目方向）
- 求职策略 / 岗位分析
- 项目方案设计

**必须执行的动作**：
1. 动笔前先用 WebSearch 搜索 2~3 次，优先采用**近 3 个月**的信息源
2. 搜索维度至少覆盖：技术趋势（新协议/新框架/范式转移）、相关框架现状（版本/生态位）、目标岗位 JD 技能要求的变化
3. 规划文档中标注「信息截至日期」，并写明依据的关键事实
4. 若搜索结果与既有计划冲突：**主动指出过时点并更新对应文件**，不许默默沿用旧结论
5. 回复用户时列出「因新知识而修改的决策」清单

**已知时效警示**（写入于 2026-09-03，本身也会过时，需定期复核）：
- naive RAG（切块 + 向量 + 生成）是 2023 年技术，2026 年只是面试入场券；差异化在 agentic RAG、GraphRAG、重排序、RAG 评测
- 行业主线已从 Prompt Engineering 转向 **Context Engineering**（上下文工程）
- **MCP** 是工具调用事实标准（2026-07 最终规范落地，SDK 月下载近亿）；**A2A** 是 Agent 间协作协议——口诀「MCP 调工具，A2A 调 Agent」
- **LangGraph 1.x** 已取代裸 LangChain 成为生产编排标准；Claude Agent SDK 是新崛起力量
- 面试高频三范式：ReAct / Plan-and-Execute / Reflection；协议对比：MCP vs Skill vs A2A

## 规则 2：records 存档规则（用户 2026-09-02 指定）

修改用户代码时不覆盖 records 文件：最终版置于文件最上方（唯一可执行代码），历史版本以 `# ══ 第N版（错误存档）❌ ══` 注释块按新到旧排在下方，关键行标 `← 教训`。历史版本必须保持注释态，否则判题会跑到旧版。

## 规则 3：辅导方式（用户长期偏好）

- **不跑判题（用户 2026-09-04 指定）**：AI 不再运行 judge.py 判题，收到代码后直接静态点评；通过与否以用户自己在力扣/本地跑的结果为准；records 存档、notebook 记录、plan 打卡照常执行
- **notebook 收录标准（用户 2026-09-04 修订）**：所有做的题都记录——磕绊的题详记（错因+坑），一次通过的题记「思考复盘」；每条支持多次复盘（⬜ 逐次打勾）
- **⭐ 做题静默规则（用户 2026-09-05 指定，最高优先级）**：用户做题时，AI **不主动给任何思路、提示、方向**——哪怕是最小的引导问题也不给。流程：① 用户拿题后完全独立思考并写码（AI 静默等待）；② 用户交代码后 AI 才点评；③ 用户主动喊"没思路/卡住"时，AI **先诊断再提示**：先问三连（题目要什么输出？输入有什么特征？你觉得哪个模式沾边？），基于他的回答给最小提示，绝不直接给方向；④ 用户长时间（>30 分钟）零进展且明确求助，才给第一个阶梯提示。禁止提前剧透"这题的坑在哪"——坑要让他自己踩
- 思考题让用户自己答；patterns.md 由用户自己写，AI 只润色（代录入需说明）
- 每题收尾固定动作：≥1 道思考题 + 同族题思路整理
- 判题用例必须含边界坑（空串、单元素、全相同等）
- Windows GBK 终端：judge.py 的 stdout UTF-8 reconfigure 勿删
